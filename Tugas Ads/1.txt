1.Program_konversi_suhu_ke_celcius
2.Deklarasi
Integer Farenheit(F),Celcius(C),N;
3.READ "Besar Temperatur" value type farenheit(F) and celcius(C);
4. IF conversion Farenheit to celcius then;
	CALCULATE C =( N - 32)*(5/9);
else conversion Celcius to Celcius then;
	CALCULATE C -N
5. DISPLAY "Celcius"